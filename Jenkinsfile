pipeline {
    agent
        stages {
        stage('clean workspace') {
            echo 'clean workspace'
        }
        stage('checkout') {
            echo 'checkout'
        }
        stage('build') {
            echo 'build'
        }
        stage('push') {
            echo 'push'
        }
        stage('deploy') {
            echo 'deploy'
        }
    }
}